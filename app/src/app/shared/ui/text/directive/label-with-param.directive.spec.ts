import { LabelWithParamDirective } from './label-with-param.directive';

describe('LabelWithParamDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelWithParamDirective();
    expect(directive).toBeTruthy();
  });
});
