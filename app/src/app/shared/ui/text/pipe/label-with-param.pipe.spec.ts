import { LabelWithParamPipe } from './label-with-param.pipe';

describe('LabelWithParamPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelWithParamPipe();
    expect(pipe).toBeTruthy();
  });
});
